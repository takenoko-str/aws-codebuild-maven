version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn install
    artifacts:
      files:
        - aws-codebuild-maven.war
      base-directory: target
      discard-paths: yes
